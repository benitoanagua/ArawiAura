---
import { translations, type Language } from "../../utils/i18n";

export interface Props {
  lang: Language;
}

const { lang } = Astro.props;
const t = translations[lang];
---

<footer class="border-t border-outlineVariant mt-20">
  <div
    class="container mx-auto py-8 px-6 text-center text-sm text-onSurfaceVariant"
  >
    <p class="italic mb-2">{t.quote}</p>
    <p>{t.rights}</p>
  </div>
</footer>
